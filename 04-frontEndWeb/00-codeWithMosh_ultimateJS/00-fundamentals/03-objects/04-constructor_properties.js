// Every object has a property. This property is called a constructor.
// The constructor references the function that was used to create that object
// Examples:

let num = 1;

console.log(num.constructor);
console.log(true.constructor);
console.log("st_ing".constructor);
