/*
 * Constants are permanent containers for data in computer memory
 * They cannot be reassigned
*/

const interestRate = 0.3;
interestRate = 1; // Gives an error for attempting to reassign a constant
console.log(interestRate)