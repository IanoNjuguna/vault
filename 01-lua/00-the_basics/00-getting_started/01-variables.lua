-- assigning variables

local score = 100
print(score)

-- Variables don't hold a type. The value in the variable has a type (Lua is dynamically typed).

-- 7 Lua Data types:
-- nil (similar to undefined in JS), strings, numbers, functions, CFunctions, userdata, table.

-- type() exists

-- string formatting
print("The value of score is "..score)

local name = "Iano"
print("The value of name is "..name)
